{
  "name": "gitmir-codex",
  "version": "1.0.0",
  "description": "GitMir for Codex: local Control dashboard + installable Codex plugin (living product model, verified task queue).",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://gitmir.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gitmir-hello/gitmir-codex.git"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.8.0"
  },
  "comments": {
    "dependencies": "Empty, and staying that way. ELK and the fonts are vendored under vendor/ so the tool has no supply chain to trust and works offline.",
    "devDependencies": "typescript is for `npm run typecheck` only. Nothing is installed to RUN this \u2014 Node executes the .ts files directly, so `git clone && node server.ts` still needs nothing.",
    "engines": "Node strips TypeScript types natively from 22.18. Node 18 and 20 are both past end of life."
  }
}
